# 混剪模式功能说明

## 新增功能概述

v0.1.2 版本新增了**纯混剪模式**，支持无文案、无配音，直接生成 MP4 视频文件。

## 两种模式对比

### 1. 完整视频模式（原有功能）
- ✅ 生成 AI 文案
- ✅ 生成 TTS 配音
- ✅ 组装视频片段
- ✅ 输出剪映草稿
- 📁 输出位置：剪映草稿箱

### 2. 纯混剪模式（新增功能）
- ❌ 无文案
- ❌ 无配音
- ✅ 组装视频片段
- ✅ 直接导出 MP4
- 📁 输出位置：自定义文件夹（默认 `output_videos/`）

## 使用方法

### GUI 界面使用

1. **第一步：素材选择**
   - 选择素材目录
   - 填写客户名称

2. **第二步：模式设置**
   - 选择生成模式：
     - ○ 完整视频模式（文案+配音+剪映草稿）
     - ● 纯混剪模式（无文案无配音，直接MP4）
   - 设置视频时长：15-30秒 或 30-60秒
   - 设置生成数量：1-10个

3. **第三步：生成视频**
   - 点击"生成草稿"或"生成视频"按钮
   - 等待处理完成

### 路径设置

- **剪映草稿箱路径**（完整视频模式）
  - 点击顶部"草稿箱设置"按钮
  - 选择剪映草稿箱路径
  - 应用会自动记住

- **混剪输出路径**（纯混剪模式）
  - 点击顶部"混剪输出设置"按钮
  - 选择输出文件夹
  - 应用会自动记住
  - 默认路径：`output_videos/`

## 技术特性

### 智能素材复用
- 优先使用历史上用得少的素材
- 同一视频内避免重复使用同一素材
- 通过随机化让每个视频的素材组合不同
- 记录使用次数，越用越少的素材优先级越高

### 随机片段时长
- 每个素材片段 3-4 秒之间随机
- 让视频节奏更自然，不会太机械
- 如果素材本身不足 3 秒，则使用素材实际时长

### 批量生成
- 支持一次生成 1-10 个视频
- 每个视频使用不同的随机种子
- 确保每个视频的素材组合和片段时长都不同

### 输出文件命名
- 格式：`{客户名称}_mix_{序号}_{时间戳}.mp4`
- 例如：`名点工贸_mix_1_20260505_143022.mp4`
- 自动避免文件名冲突

## 文件结构

```
python_video_engine/
├── src/
│   └── python_video_engine/
│       ├── material_fetcher.py      # 素材扫描
│       ├── content_generator.py     # 文案生成（完整模式）
│       ├── assembly_engine.py       # 片段组装
│       ├── draft_renderer.py        # 草稿渲染（完整模式）
│       └── video_exporter.py        # MP4 导出（混剪模式）⭐新增
├── main.py                          # GUI 和 CLI 入口
├── output_videos/                   # 混剪视频输出（默认）
└── output_drafts/                   # 剪映草稿输出
```

## 依赖说明

混剪模式使用 `moviepy` 库进行视频拼接和导出：
- `VideoFileClip` - 加载视频片段
- `concatenate_videoclips` - 拼接多个片段
- `write_videofile` - 导出 MP4 文件

编码参数：
- 视频编码：`libx264`
- 音频编码：`aac`
- 帧率：30 fps

## 注意事项

1. **首次使用**
   - 完整视频模式需要先设置剪映草稿箱路径
   - 纯混剪模式需要先设置混剪输出路径

2. **素材要求**
   - 素材文件夹结构必须包含：
     - `01_工厂全景与大环境`
     - `02_机器运转与加工细节`
     - `03_成品展示与发货`
   - 素材格式：MP4

3. **性能考虑**
   - 混剪模式比完整模式快（无需 AI 生成和 TTS）
   - 视频导出速度取决于素材数量和时长
   - 批量生成时，视频之间有 2 秒间隔

4. **磁盘空间**
   - 每个混剪视频约 10-50 MB（取决于时长和素材质量）
   - 确保输出路径有足够空间
